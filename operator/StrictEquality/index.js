// = assignment operator
// == comparison operator (compare if values are equal) 
//    So sánh nếu các giá trị bằng nhau sau khi thực hiện ép kiểu
// ex. `5 == "5";` trả về true ( chuỗi '5' được ép kiểu thành số 5 trước khi so sánh

// === strict equality operator (compare if values & datatypes are equal)
// != inequality operator
// !== strict iequality operator

